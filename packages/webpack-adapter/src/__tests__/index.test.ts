import { describe, expect, test } from 'vitest';

import { internal_createWebpackConfigForImportConditions } from '../index.ts';

describe('@TODO', () => {
  test('internal_createWebpackConfigForImportConditions exists', async () => {
    expect(internal_createWebpackConfigForImportConditions).toBeTruthy();
  });
});
